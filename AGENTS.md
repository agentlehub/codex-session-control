# Codex Compatibility

To change the supported Codex version, use `./scripts/set-supported-codex-version.sh VERSION`.

# Pull Requests

Use `.github/PULL_REQUEST_TEMPLATE.md` when creating or updating a pull request.
